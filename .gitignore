node_modules/
*.log

# arc-lint reports
arc-lint.json
arc-lint.sarif
reports/

# Mirrored upstream documentation. Regenerate with:
#   npm run mirror-docs
#   npm run mirror-docs -- --site circle
# Comment these out to vendor the mirror into the repo instead.
docs/
docs-circle/

# Build output for npm. The repo runs TypeScript directly; only published
# packages need compiling, because Node refuses to strip types in node_modules.
packages/*/dist/
*.tgz
